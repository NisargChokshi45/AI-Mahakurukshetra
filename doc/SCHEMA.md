# SCHEMA

No database schema has been committed yet.

Planned initial areas:
- `profiles`
- `projects`
- `messages` or `threads` for AI interaction history
- `subscriptions` only if billing is included in the first implementation slice
