Purpose: Enforce consistent coding standards across the project.

Standards:
- File naming conventions (PascalCase components, camelCase utilities)
- TypeScript rules (no any, use Zod for runtime validation)
- React patterns (Server Components by default, colocate related files)
- Commit message format (via git-commit skill)